<div>
        <% book.book_comments.each do |book_comment| %>
        <table>
          <tr>
            <td><%= image_tag book_comment.user.get_profile_image,size: "100x100" %></td>
            <td><%= book_comment.comment %></td>
            <td><% if book_comment.user == current_user %>
            <%= link_to "destroy", book_book_comment_path(book, book_comment), method: :delete,remote: true, class: "btn bnt-sm btn-danger" %>
            <%end%>
            </td>
          <%end%>
          </tr>
        </table>
      </div>

